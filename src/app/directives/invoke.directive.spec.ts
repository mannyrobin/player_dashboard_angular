import { InvokeDirective } from './invoke.directive';

describe('InvokeDirective', () => {
  it('should create an instance', () => {
    const directive = new InvokeDirective();
    expect(directive).toBeTruthy();
  });
});
